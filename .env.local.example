# Convex Backend (ALWAYS REQUIRED)
# Get these from https://dashboard.convex.dev
CONVEX_DEPLOYMENT=dev:your-deployment-name
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# Better Auth (ALWAYS REQUIRED)
# Generate secret with: openssl rand -base64 32
BETTER_AUTH_SECRET=
SITE_URL=http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# AI Provider (Agent Component)
# Choose ONE provider below:

# Option 1: OpenAI (recommended for most use cases)
OPENAI_API_KEY=

# Option 2: Anthropic Claude (alternative)
# ANTHROPIC_API_KEY=

# Rate Limiter Component
# No configuration needed - works out of the box
